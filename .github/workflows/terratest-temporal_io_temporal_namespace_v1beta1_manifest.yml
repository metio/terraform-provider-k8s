# SPDX-FileCopyrightText: The terraform-provider-k8s Authors
# SPDX-License-Identifier: 0BSD

name: TemporalIoTemporalNamespaceV1Beta1Manifest
on:
  pull_request:
    branches: [ main ]
    paths:
      - examples/data-sources/k8s_temporal_io_temporal_namespace_v1beta1_manifest/**
      - internal/provider/temporal_io_v1beta1/temporal_io_temporal_namespace_v1beta1_manifest.go
      - internal/provider/temporal_io_v1beta1/temporal_io_temporal_namespace_v1beta1_manifest_test.go
      - terratest/temporal_io_v1beta1/temporal_io_temporal_namespace_v1beta1_manifest_test.go
jobs:
  terratest:
    name: Test with Terraform ${{ matrix.terraform }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        terraform:
          - 1.4.*
          - 1.5.*
    steps:
      - id: checkout
        name: Checkout
        uses: actions/checkout@v3
      - id: setup_go
        name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version-file: go.mod
          cache: true
      - id: setup_terraform
        name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ matrix.terraform }}
          terraform_wrapper: false
      - id: install
        name: Install Provider
        run: make install
      - id: tests
        name: Run Unit Tests
        run: go test -v -timeout=120s -count=1 ./internal/provider/temporal_io_v1beta1/temporal_io_temporal_namespace_v1beta1_manifest_test.go
      - id: terratest
        name: Run Terratest Tests
        run: go test -v -timeout=120s -count=1 -run TestTemporalIoTemporalNamespaceV1Beta1Manifest ./terratest/temporal_io_v1beta1
