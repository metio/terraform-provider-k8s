# SPDX-FileCopyrightText: The terraform-provider-k8s Authors
# SPDX-License-Identifier: 0BSD

name: WorkKarmadaIoResourceBindingV1Alpha2DataSource
on:
  pull_request:
    branches: [ main ]
    paths:
      - examples/data-sources/k8s_work_karmada_io_resource_binding_v1alpha2/**
      - internal/provider/work_karmada_io_v1alpha2/work_karmada_io_resource_binding_v1alpha2_data_source.go
      - terratest/work_karmada_io_v1alpha2/work_karmada_io_resource_binding_v1alpha2_data_source_test.go
jobs:
  terratest:
    name: Test with Terraform ${{ matrix.terraform }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        terraform:
          - 1.4.*
          - 1.5.*
    steps:
      - id: checkout
        name: Checkout
        uses: actions/checkout@v3
      - id: setup_go
        name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version-file: go.mod
          cache: true
      - id: setup_terraform
        name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ matrix.terraform }}
          terraform_wrapper: false
      - id: install
        name: Install Provider
        run: make install
      - id: terratest
        name: Run Terratest Tests
        run: go test -v -timeout=120s -count=1 -tags testing -run TestWorkKarmadaIoResourceBindingV1Alpha2DataSource ./terratest/work_karmada_io_v1alpha2
